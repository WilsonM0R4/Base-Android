<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bg_gradiente</item>
        <item name="colorPrimaryDark">@color/bg_gradiente</item>
        <item name="colorAccent">@color/orange</item>
        <item name="android:dialogTheme">@style/Dialog.Theme</item>
        <item name="android:actionBarStyle">@style/AppActionBar</item>
        <item name="actionBarStyle">@style/AppActionBar</item>
        <item name="colorControlNormal">@color/orange</item>
        <item name="colorControlActivated">@color/orange</item>
        <item name="colorControlHighlight">@color/orange</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/visacheckoutsplash</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="Dialog.Theme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/bg_gradiente</item>
        <item name="colorPrimaryDark">@color/bg_gradiente</item>
        <item name="colorAccent">@color/bg_gradiente</item>
    </style>

    <!-- ActionBar styles -->
    <style name="AppActionBar" parent="Widget.AppCompat.Light.ActionBar">
        <item name="android:background">@color/bg_gradiente</item>
        <item name="background">@color/bg_gradiente</item>
        <item name="android:titleTextStyle">@style/TitleBarTextColor</item>
        <item name="titleTextStyle">@style/TitleBarTextColor</item>
    </style>

    <style name="TitleBarTextColor" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/bg_gradiente</item>
    </style>

    <style name="MainTheme" parent="@android:style/Theme.NoTitleBar.Fullscreen" />

  <!--  <style name="AppTheme" parent="@android:style/Theme.Holo.Light">

        <item name="android:textColorPrimary">#ff0000</item>
    </style>-->

    <style name="MyEditTextStyle">

    <item name="android:textColor">@color/register_input_active</item>
    </style>

</resources>
