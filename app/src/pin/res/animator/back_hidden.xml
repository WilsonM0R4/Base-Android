<set xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Move right. -->
    <objectAnimator
        android:valueTo="-250f"
        android:valueFrom="0f"
        android:propertyName="x"
        android:duration="@integer/card_flip_time_half"/>

</set>
